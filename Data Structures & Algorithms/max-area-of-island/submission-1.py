class Solution:
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        
        maxarea=0
        netcol=len(grid[0])
        netrow=len(grid)
        visited=[[0]*netcol for _ in range*networ]
        print(visited)
        return 2
